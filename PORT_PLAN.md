# 豆卜·离线精简版（小红书vibecoding大赛参赛版）— 移植计划

> 0903 立项。截止 **2026-09-07 23:59**。分工：探春造包，lyvia 全手动上传/发笔记。
> 时间线：9/4 可玩版＋≥10卦一致性对照 → 9/5 审计通过整包＝最晚提审 → 9/6-9/7 发笔记。

## 目录约定
- `ref/liuyao.py`（743 行）＋ `ref/algorithm.py`（2489 行）＝服务器线上引擎快照（/home/server/apps/idol-astro/，0903 拉取）。**移植对照源，禁直接改线上。**
- `vendor/minitool-skill/`＝小红书官方打包规范 **v1.6.0**（SKILL.md＋7 份 references＋审计脚本）。`minitool-skill-1.2.0.bak/` 是旧版，**以 1.6.0 为准**。同内容已装 `.claude/skills/minitool-zip-builder`。
- `src/`（待建）＝源码；`dist/`＝打包产物目录；`tool.zip`＝最终交付。

## 架构（已锁死）
纯前端离线小工具：选心事分类 → 按住-松手摇卦（禁传感器）→ 完整排盘（本卦/变卦/六亲/六神/用神/标记列/公历应期）→ 本地规则白话解读。**无 LLM、无网络、无付费**（lyvia 0903 确认＋大赛红线）。简体默认、紫金宣纸视觉（纯 CSS，禁大图）。JSBridge 增强（卦卡存相册/发笔记）做成能力检测可选，不挡基础功能。

## 移植范围（liuyao.py 为主，algorithm.py 只搬用到的部分）
- 必搬：BAGUA 八卦表、64 卦表、装卦（纳甲/六亲/六神）、干支历（日柱/月柱＋旬空）、用神选取、旺衰评分、应期（公历日期化）
- 起卦交互改造：线上是三数起卦（a,b,c），小工具版改「按住-松手」取随机三数（本地 Math.random 即可，无合规问题），底层仍走同一装卦管线
- 不搬：评分星级文案里的追星语境、idol 相关分支（0828 铁律：非追星卦零追星词，参赛版整体走文化科普语境）
- 解读文案库：新建本地规则库（分类×用神旺衰×动爻 → 白话段），措辞过 0828 违禁词表（算命/运势/保佑/化解/旺财/招财禁），交稿前零克查词复扫

## 1.6.0 编码铁律（写码时逐条守，详见 vendor SKILL.md）
1. 经典脚本：禁 `type="module"`/import/export/top-level await；多文件按依赖顺序 `<script src>`，window 命名空间
2. JS 只写 ES2017：禁 `?.`/`??`/对象 spread/逻辑赋值/BigInt；async/await、padStart、includes 可用；replaceAll/Array.at/Object.hasOwn/structuredClone 禁作唯一路径
3. CSS Chrome 61 基线：flex gap 用 margin 基线（行为检测后增强）；无 aspect-ratio/clamp/min/max/:has/dvh/oklch/color-mix；物理属性；overflow:hidden；backdrop-filter 只作增强
4. 日期：`new Date(y, m-1, d)` 构造，禁字符串解析；业务顺序用数组禁靠对象遍历序
5. 事件全 addEventListener；viewport 带 `viewport-fit=cover`＋安全区 env() fallback；布局 flex/% 禁写死宽；系统字体栈
6. 包体：zip ≤10MiB 硬上限、目标 <2MiB；单图 <500KB；base64 单条 <100KiB
7. 交付前：`python3 vendor/minitool-skill/scripts/audit_artifact.py dist` 与 `… tool.zip` 各跑一遍，ERROR 清零才交

## 验证门
- ≥10 卦与线上真卦对照一致（同输入→同本卦/变卦/六亲/应期）＋纯汉字字数、繁简、违禁词扫描
- 表单文案（≤14 字，lyvia 直接粘）：名称「豆卜·六爻解忧」候选／简介候选随 9/5 整包给

## 状态
- [x] 官方规范 1.6.0 读毕＋审计脚本 smoke test PASS（0903）
- [x] 引擎源码快照入 `ref/`（0903）
- [x] 引擎 JS 移植（`src/engine.js`，含应期日历）＋双端全量对照 **1152 例全绿**（64卦×6动爻×3日期×5用神；字段对照 0 差异＋应期逐例性质校验 0 差异；`python3 tests/consistency_check.py` 可复跑）（0903）
- [x] 对齐线上怪癖：moving_relation 按默认用神「妻财」计算且覆盖用神后不重算——1:1 保留（0903）
- [x] 解读文案库（`src/readings.js`，违禁词 0 命中）＋宣纸风 UI＋按住-松手摇卦（三枚铜钱掷六次，无动爻自动重摇）＋卦史（localStorage 上限 30）＋JSBridge 能力检测（存相册/发笔记，无 bridge 回退长按预览）（0903）
- [x] 打包＋审计：`tool.zip` 23KB，官方审计 dist/zip 双 PASS 0 warning；真浏览器 E2E 15/15（Chrome headless，含卦卡体积门 90KB<100KiB）；违禁词 29 词 0 命中（0903）
- [x] 提审材料：`提审材料.md`（表单逐字文案/自查表/AI 声明/笔记初稿）＋ `icon_512.png`（PIL 宋体紫金款）；整包已复制 `~/Public/红楼读书会/📤 我们给老杨的/豆卜小工具-提审包-0903/`（0903）
- [ ] lyvia 上传提审（9/5 前）→ 过审后 9/6-9/7 发笔记（标题带「小红书vibecoding大赛」）
- [x] 赛道已定 #vibegame 互动游戏（lyvia 0906 拍板）；待 lyvia：v42 包＋印章图标传 Builder Hub、9/6-9/7 发笔记
- [x] 四十八轮（0907）：lyvia 真机报「结果页大字报只剩一行字，点开才有」→ 根因=小红书容器拦 data: 内嵌图，0904 只给弹层加了 onerror 兜底、内嵌没加 → renderResult 补克隆画布兜底（克隆非共用，弹层 previewCard 会搬走 state.posterCanvas）＋点击区从 img 扩到整卡＋CSS canvas 同款式＋handler 触发后自摘防叠。验证：e2e 133 全绿（顺带修了「判断句高亮结论」假挂——起卦含当日月建，断言钉死旧文案，改机制断言）＋新增 tests/fallback_probe.html 6 项真机时序探针全 PASS＋审计 dist/zip 双 PASS → **tool.zip v43 46.6KB 已发**；提审材料同步。待 lyvia：删 v42 传 v43＋扫码复测
- [x] 四十九轮（0907）：她报「大字报还是保存不了」＋问权限能不能存图时再问 → ①规范（vendor jsbridge-api.md）推荐 writeTempFile 落临时文件再传 filePath，原直传 data:uri 属允许非推荐 → saveToAlbum() 走规范路径，无 writeTempFile 回退直传，失败 toast「没存上，可截图分享」；②权限澄清：官方规范「需由用户主动操作触发」——表单勾权限只是静态声明不弹窗，弹窗只在点保存那刻；⚠️若表单选了「不需要权限」容器会拒存图调用=「保存不了」主嫌疑，提审材料已加粗改写。验证：stub 桥探针 10 项全 PASS（writeTempFile 收完整 data:uri/相册 API 收临时路径/成功安静/失败提示）＋e2e 133 全绿＋审计双 PASS → **tool.zip v44 46.9KB 已发**。待 lyvia：表单勾相册权限＋删旧包传 v44＋扫码复测存图
- [x] 五十轮（0907）：她报「上架了还是存不了图片 只能分享 好没用啊 没有分享属性」→ ①规范§4「系统长按菜单已禁用」=结果页「长按保存大字报」提示是误导，改「点大字报可存相册或发笔记」；②存图失败 toast 带 errMsg 尾段（5200ms）→ 真机截图即可远程诊断；③弹层新增「发笔记分享」（HAS_POST 亮，点= postNote(previewUri, photo_publish)）→ 存不了图也有分享出口。验证：probe 13 项全 PASS（含 postNote 收 photo_publish/发笔记按钮已亮）＋e2e 132 全绿＋审计双 PASS → **tool.zip v45 47.4KB**。⚠️dist 曾混入 3 个 .bak 致 zip 9 文件，已 rm＋重建（教训再证：进 zip 前必查 unzip -l）
- [x] 五十一轮（0907）：她发 v45 真机截图——toast=「可截图分享」（无桥分支文案），不是「没存上(xxx)」（接口失败文案）→ **app 在载入那一刻根本没看到 window.xhs.miniTool**。真凶候选＝容器晚注入桥，而 app.js 载入时快照 HAS_SAVE/HAS_POST 一次定生死 → 永远 false。v46：①mt()/hasSave()/hasPost() 点击时现查，saveToAlbum/postNoteWith 内部现取；②查不到 → toast 报缺层「没找到存图接口(xhs未注入/miniTool未注入/接口名缺)，可截图分享」5200ms；③弹层「发笔记分享」常亮（v45 在无桥时会藏掉，与提示文案自相矛盾），点了没接口给诊断。验证：新增 tests/late_bridge_probe.html（先无桥走一遍→诊断 toast 准确；运行中注桥→writeTempFile→相册→postNote 全通）7/7 PASS＋e2e 132 绿＋存图链探针 12 绿＋审计 PASS → **tool.zip v46 47.9KB 已发**。判读表：她真机 v46 点存图 → 「没找到存图接口(xhs未注入/miniTool未注入)」=容器这入口不给桥（找官方/截图+挂载兜底）；「没存上(xxx)」=接口在调用失败（按 errMsg 修）；直接成功=晚注入实锤结案
- [x] 五十二轮（0907）：她 v46 真机截图 toast=「没找到存图接口(xhs未注入)，可截图分享」→ **诊断闭环：window.xhs 整个没注入**＝这版内测容器不提供任何 JSBridge（存相册/发笔记都没有；官方能力清单也确实只给 alert/confirm，一个桥接口都没列）。包侧无解，收口动作：①无桥 toast 改用户视角（「这版容器还没开存图接口，截图即可保存」），报错腔退役，mtDiag 删；②弹层提示随桥自适应（无桥=「截图即可保存这张图」不悬空承诺发笔记）。验证：e2e 132 绿＋存图链探针 12 绿＋晚注入探针 9 绿＋审计 PASS → **tool.zip v47 48.0KB 已发**。余下两条腿都在容器/官方侧：她更新小红书最新版再试（若新版注入桥=白捡）＋内测群问官方有无存相册/发笔记接口。大赛交付不受影响：截图即存＋笔记挂载是官方主路径
- [x] 六十轮（0910，lyvia 专业度四连改）：入口按钮「手动起卦」＋描述行；标题去橙（墨色加粗主标题/品牌紫副行标签）；选看法两列 grid；发笔记第三张=卦史页完整内容（八卦图鉴64格＋最近卦史）。验证：e2e 170＋存图链 13＋晚注入 9 全绿，审计 PASS，tool.zip v55=54775B；第三张图已从 e2e 导出解码核验，390px 输入页/选看法页截图核验。余：她删旧传 v54 传 v55 真机复测
- [x] 五十九轮（0910，lyvia 定稿☆版）：装卦说明换她定稿☆版；交互照豆卜原版重做（符号切阴阳/右侧☆标动/动爻唯一顶替）；每爻符号旁小字状态标（.yao-mini 少阳/少阴/老阳/老阴）；子说明随模式切换（装卦☆版/摇卦○✕版）。验证：e2e 169＋存图链 13＋晚注入 9 全绿（探针不经过手动视图未重跑），审计 PASS，tool.zip v54=54198B；390px 装卦/摇卦截图核验。余：她删旧传 v53 传 v54 真机复测
- [x] 五十八轮（0910，lyvia 摇卦功能再改）：手动起卦入口挪进输入卡（数字起卦下方、开始鉴定上方，同显眼）；进来默认即时摇卦；排盘倒转=上爻在顶初爻在底（renderManual for i=5→0，与结果表/卦盘图对齐；⚠️renderManual 每次点击 innerHTML 全量重建，测试断言必须重查 NodeList 否则抓到游离节点）；「直接装卦/即时摇卦」改名；摇卦/装卦说明按她定稿（装卦两行版 white-space:pre-line）。验证：e2e 163＋存图链 13＋晚注入 9 全绿，审计 PASS，tool.zip v53 52.5KB（53765B）；390px 三态截图（输入页/摇卦空盘/装卦两爻）核验通过。余：她删旧传 v52 传 v53 真机复测
- [x] 五十七轮（0910，lyvia 看真机截图再改）：主按钮改「发笔记保存全部历史图鉴」（postNote 三张打包不变）；「只存图」拆两个单存 ghost「存本次完整卦盘」（onSaveCardImg）＋「存本次大字海报」（onSavePosterImg），saveAllToAlbum 退役；「再摇一卦」→「重测一次」；「卦图鉴（仅存本机）」→「八卦图鉴」；弹层提示同步。验证：e2e 161＋存图链 13＋晚注入 9 全绿，审计 PASS，tool.zip v52 52.3KB；390px 静态台验层级无溢出。余：她删旧传 v52 真机复测

- [x] 五十六轮（0909，lyvia 口述重做）：①发笔记文案——「没有人想要因为玩了一个工具去发小红书的」→标题固定「竟然，我的CP竟然是这种关系」（悬念；非CP=「我摇出一卦：「verdict」」，20字截断），正文=邀请「你来测测你磕的CP是真是假吧👇小工具就挂在这篇笔记下面，点开就能测」，撤卦曰/金句/卦象/卦史正文；引用她原笔记=她发布页手动操作。②按钮——「发笔记 · 三张图全带走」升主按钮（实心紫，postNote 三图打包 poster+card+history），「只存图 · 三张进相册」降 ghost 次按钮（saveAllToAlbum 三张安静连存+汇总 toast「三张都已保存到相册」/「N张没存上，可截图或发笔记」），「存完整卦盘」单存按钮撤并；点大字报仍=预览+单存。验证：e2e 160＋存图链 13（打包=4写4存）＋晚注入 9 全绿，审计 PASS，tool.zip v51 52.3KB；静态 390px 台核按钮层级（主实心+ghost 行 342≤354 不溢出）。余：她删旧版传 v51 真机复测

- [x] 五十五轮（0909，lyvia 拍板）：豆卜「手动起卦」迁移——史同/虚拟人物没有生日数字，数字起卦无从下手（对标笔记评论区同款痛点）。输入页新入口「想要更准？手动起卦（掷硬币六爻 / 直接装卦）→」→ viewManual：✋装卦=六行四态轮换（阳→阴→老阳○→老阴✕，引擎 castFromLines 单 moving 参数锁死→限选一动后选顶替）＋🎲摇卦=三铜钱掷六次（3背○/2背少阳/1背少阴/3字✕，六静→「再摇一轮」原地重摇，摇完可切回装卦微调）。管线复用：manualGo 组 {lines,moving}→state.manualCast→rollNow(pre) 消费，castSource='manual'，纳甲/世应/应期/图鉴/卦史全链不变；头部小字三处改 srcSuffix()（手动卦标「· 手动起卦」）。验证：e2e 154 绿（新增手动 22 断言：四态/顶替唯一/静卦拦/摇六/微调/入史）＋存图链 12＋晚注入 9＋审计 PASS，tool.zip v50 52.0KB；390px 截图台核对两态排版（flex 行对齐/文案换行/按钮态）。余：她删旧版传 v50 真机复测
- [x] 五十四轮（0909）：她真机四痛点一次修完（**好消息：接口已在她客户端注入，发笔记草稿「已添加小工具」**）：①图糊根治=2倍分辨率直出（大字报1440×1920/卦盘1440×N，撤600宽+100KB旧红线放宽1.2MB）；②存图成功必 toast「已保存到相册」（原静默靠权限弹窗，已授权就不弹）；③「发笔记」→「存成图片发笔记」＋卦盘按钮间距14→28px；④发笔记重做=三图打包（大字报+卦盘+新增 drawHistoryPoster 测算历史海报：已收录N/64·嗑过N对·最近7条）+钩子文案（「我磕的CP算出「verdict」」+金句+卦史引导）。验证：e2e 132＋存图链12＋晚注入9 全绿，tool.zip v49 49.4KB 已发。追加：主按钮「生成卦辞图片分享」→「保存卦辞图片」（全 harness 同步改＋e2e 断言随改；溢出断言改「无元素越界」语义，诊断实测 sw=741<iw=756 本无溢出）。余：她删旧版传 v49 真机复测（图清晰度/存图toast/三图笔记/按钮文案）
- [x] 五十三轮（0907）：她质疑「代码肯定有问题，我已选了授权图片」＋发来姜sir笔记（官方「昨天刚更新」postNote/saveImageToPhotosAlbum/writeTempFile 三接口）→ 拉到官方《容器能力清单》CDN 原文核对：①入口 window.xhs.miniTool.*、签名与我们完全一致（授权没白做，接口注入即可用）；②官方自认「调用前判空，为未注入的环境准备降级路径」＝未注入是官方承认的环境态；③「img 加载 data:/blob: 需客户端 9.37+」＝端能力有版本门槛。代码动作：mt() 焊死不赌名字（官方入口优先＋全窗口扫双方法对象），late probe 阶段B改注 zqqOddBridge 验证 9/9 绿；新增 env_probe 探针包（UA/候选名单/三接口存在性）。判读：探针「不存在」→ 客户端版本/灰度，更新小红书 App＋拿截图问官方；「存在」→ 按 errMsg 接着修。验证：e2e 132＋存图链 12＋晚注入 9 全绿，tool.zip v48 48.3KB＋env_probe.zip 双审计 PASS 已发。⚠️本环境 cat heredoc 两次写出 0 字节文件（msg50.txt/env_probe 首版），写文件一律 python
- [ ] 待 lyvia（五十轮后）：①确认上架传的是 v45 zip（47.4KB，弹层里要有「发笔记分享」按钮=版本指纹）；②提审表单确认勾了「保存到相册」类权限；③真机点存图，把新 toast「没存上(xxx)」里的 xxx 截图发我 → 按 errMsg 定位（denied=权限没勾/review 不够=平台限制/其他=拿去找官方）
